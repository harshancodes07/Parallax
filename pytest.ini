[pytest]
pythonpath = backend
# Both suites. `tests/` is the tutor branch, `backend/tests/` is RAG — leaving
# either out means a green run that proved half of what you think it did.
testpaths = tests backend/tests
markers =
    slow: loads a real model (embeddings); skip with -m "not slow"
